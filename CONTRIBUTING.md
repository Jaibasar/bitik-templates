# Ережелер

Бұл жобаға өзгеріс қосу немесе оны басқаруда ұстанатын қарапайым
ережелер тізімі.

1. **Архив/backup файлдарын commit жасамаңыз.** Git өзі толық
   тарихты сақтайды — жобаны `.zip` етіп қайта қосудың қажеті жоқ
   (`.gitignore` мұны автоматты түрде елемейді).
2. **Компиляция қалдықтарын қоспаңыз.** `*.aux`, `*.log`, `*.toc`
   және `sizes/` ішінде құрастырылған PDF-тер репозиторийге кірмейді
   — тек `samples/` қалтасындағы дайын демо-PDF-тер ғана commit
   жасалады.
3. **Жаңа бет өлшемін қосу үшін** `sizes/` қалтасына жаңа `.tex`
   файлын жасап, README-дегі 8 негізгі айнымалыны (`\trimwidth`,
   `\trimheight`, `\margintop`, `\marginbottom`, `\margininside`,
   `\marginoutside`, `\basefontsize`, `\baselinestretchval`,
   `\booktitle`, `\bookauthor`) анықтап, соңында
   `\input{../bitik-core.tex}` шақырыңыз.
4. **Жаңа қаріп қоспас бұрын лицензиясын тексеріңіз** — `LICENSE.md`
   файлындағы "Үшінші тарап компоненттері" бөлімін жаңартуды
   ұмытпаңыз.
5. **Құрылым өзгерсе, `README.md` мен `LICENSE.md`-ні де жаңартыңыз**
   — құжаттама іс жүзіндегі жобамен сәйкес болуы керек.
6. **Commit хабарламалары қысқа әрі нақты болсын** — не істелгенін
   емес, неге істелгенін түсіндіріңіз.
